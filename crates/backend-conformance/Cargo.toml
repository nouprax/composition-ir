[package]
name = "backend-conformance"
version = "0.0.0"
description = "Executable form of the backend contract: three stand-in output targets and the gates"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
composition-ir = { path = "../composition-ir" }

[lints]
workspace = true
