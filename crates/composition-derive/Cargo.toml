[package]
name = "composition-derive"
version = "0.0.0"
description = "Engine-internal memoization and invalidation over Composition IR"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
composition-ir = { path = "../composition-ir" }

[lints]
workspace = true
