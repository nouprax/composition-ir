[package]
name = "frontend-conformance"
version = "0.0.0"
description = "Executable form of the frontend contract: a fixture frontend, its adapter, and the gates"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
composition-ir = { path = "../composition-ir" }

[lints]
workspace = true
